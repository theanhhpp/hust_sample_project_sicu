
<div >
    <ul class="home-post">
    <% @posts = Post.all.order('created_at DESC')%>
    <% @posts.each do |post| %>
        <li>
            <%=link_to (image_tag post.image.url, class:'image'),post_path(post) %>
        </li>
    <%end%>
    </ul>   
</div>