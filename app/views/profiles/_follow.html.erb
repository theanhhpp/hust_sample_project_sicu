<%= form_for(current_user.active_relationships.build(followed_id: @user.id)) do |f| %>
  <div><%= f.hidden_field :followed_id %></div>
  <%= f.submit "Follow", class: 'btn btn-primary form-margin', style: 'opacity: 0.8;' %>
<% end %>